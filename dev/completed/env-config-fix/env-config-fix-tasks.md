# Env config fix tasks

- [x] Inspect current environment validation and related config usage.
- [x] Update environment schema to allow standard env keys and include `SUPABASE_DIRECT_URL` validation.
- [x] Refresh docs/notes and run `npm run build` to verify (resolved prior TS typing errors in `src/database/client.ts` and `src/services/consensus-calculator.service.ts`).
